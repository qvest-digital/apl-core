# Only these knobs exist. Everything else is upstream's manifest, verbatim -- see README.md.
# An empty `repository`/`tag` means "whatever the upstream manifest says".
controller:
  resources: {}
  image: {repository: "", tag: ""}
webhook:
  resources: {}
  image: {repository: "", tag: ""}
interceptors:
  resources: {}
  image: {repository: "", tag: ""}
